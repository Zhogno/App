apiVersion: apps/v1
kind: Deployment
metadata:
  name: synergy
  labels:
    app: synergy
    kubernetes.azure.com/generator: devhub
  namespace: namespace-workflow-1727251741140
spec:
  replicas: 1
  selector:
    matchLabels:
      app: synergy
  template:
    metadata:
      labels:
        app: synergy
    spec:
      containers:
        - name: synergy
          image: acrworkflow1727251741140.azurecr.io/image-workflow-1727251741140:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
